{
  "name": "api",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "air -c ./.air.toml",
    "build": "go build -o ./build/api ./cmd/api/main.go",
    "test": "go test -v ./... -coverprofile=coverage.out",
    "lint": "golangci-lint run --fix",
    "format": "go fmt ./...",
    "prepare": "go mod tidy"
  }
}
